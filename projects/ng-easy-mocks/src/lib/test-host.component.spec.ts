import { TestHost.Component } from './test-host.component';

describe('TestHost.Component', () => {
  it('should create an instance', () => {
    expect(new TestHost.Component()).toBeTruthy();
  });
});
